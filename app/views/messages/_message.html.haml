%li.message.item
  .row
    .span1
      = link_to(image_tag(message.sender.avatar_url(:thumb)))
    .span6
      %p
        = link_to(message.sender.fast_name, vanity_path(message.sender), :class => 'core')
        %span.quiet
          = ' • '
          = friendly_datetime(message.created_at)
      = simple_htmlize(message.body)
  .row
    .span7
      %hr.slim