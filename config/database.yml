development:
  adapter: postgresql
  encoding: unicode
  database: criticube_development
  pool: 5
  username: postgres
  password:

  # Connect on a TCP socket. Omitted by default since the client uses a
  # domain socket that doesn't need configuration. Windows does not have
  # domain sockets, so uncomment these lines.
  host: localhost
  port: 5432

  # Schema search path. The server defaults to $user,public
  #schema_search_path: myapp,sharedapp,public

  # Minimum log levels, in increasing order:
  #   debug5, debug4, debug3, debug2, debug1,
  #   log, notice, warning, error, fatal, and panic
  # The server defaults to notice.
  #min_messages: warning

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  encoding: unicode
  database: criticube_test
  pool: 5
  username: postgres
  password:
  host: localhost
  port: 5432  


staging:
  adapter: postgresql
  encoding: unicode
  database: da8dahahupb18q
  username: rsdopfrzcqfcul
  password: 4c-2rMsIh84M3tiCV-NeWQFLm4

production:
  host: ec2-107-20-162-239.compute-1.amazonaws.com
  adapter: postgresql
  encoding: unicode
  database: d7dd2iusp07r27
  username: ua0oai4b7qoa6
  password: pe1hopktr434113gtje246bdof